---
id: "260988b3"
title: "Make mutate command targets optional like run command"
tags: ["bug", "cli", "ux"]
---

## Problem

There's an inconsistency between `run` and `mutate` commands regarding target arguments:

- **`run` command**: Targets are optional. If not provided, it tests existing mutants in the database. If config `[targets].include` is set, it can be used for mutation generation.
  
- **`mutate` command**: Targets are **required** (`required = true` in CLI definition). Users must provide positional target arguments even if `[targets].include` is set in config file.

## Expected Behavior

Both commands should be consistent:
1. Users should be able to run `mewt mutate` with just config file settings (`[targets].include` and `[targets].ignore`)
2. CLI positional arguments should be optional and override config when provided
3. If neither CLI args nor config targets are set, show an error

## Implementation

1. Remove `required = true` from `MutateArgs.targets` in `src/core/cli.rs`
2. Update `Commands::Mutate` handling in `src/core/main_shared.rs` to check if targets are empty and fall back to config
3. Add validation to error if both CLI targets and config targets are empty
4. Update help text to clarify optional nature and precedence

## Files to Change

- `src/core/cli.rs` - MutateArgs struct
- `src/core/main_shared.rs` - Mutate command handling

## Progress

Changes made (uncommitted, staged in git):
1. ✅ Removed `required = true` from `MutateArgs.targets` in `src/core/cli.rs`
2. ✅ Updated help text to clarify targets are optional and fall back to config
3. ✅ No changes to main_shared.rs needed - existing resolve_targets() handles validation
4. ✅ Manually verified error handling when no targets provided
5. ✅ Manually verified functionality with CLI targets
6. ✅ Linting passes

## Test Failures (Pre-existing, Unrelated to This Change)

These tests fail on both dev branch and with my changes:
- test_display_single_line_replacement ... FAILED
- test_display_when_line_offset_zero ... FAILED  
- test_display_multi_line_replacement ... FAILED

Root cause: Tests in tests/display.rs don't call set_namespace() before using config system, causing panic at config.rs:222 in get_config_filename(). This is a pre-existing test infrastructure issue, not caused by my CLI changes.

My change only modifies cli.rs (removes `required = true`, adds help text). The test failures are completely independent.
