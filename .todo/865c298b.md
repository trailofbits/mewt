---
id: "865c298b"
title: "backfill-compound-assignment-slug-tests"
---

- [x] Keep mutation-agnostic coverage in `tests/<lang>/integration_tests.rs`, add mutation-specific suites under `tests/<lang>/mutations/<slug>.rs`, and create the subdirectories as needed.
- [x] Inline mutation-specific source snippets and expectations in those per-slug tests; keep shared fixtures in `tests/<lang>/examples` for general integration coverage.
- [x] Introduce a helper that mutates a source snippet, filters by a requested slug, and asserts both the expected mutants appear and no unexpected slugs slip through.
- [x] For every supported slug in each language, add a dedicated test using that helper to validate the mutants produced by a targeted snippet.
- [x] Extend the mutation-agnostic integration tests to enumerate supported slugs (via the language engine) and fail if any slug lacks a matching per-slug test file.
- [x] Audit language integration and per-slug tests to ensure AAOS/BAOS/SAOS each emit at least one mutant, including language-specific operators (`%=`/`**=`/`&^=`/`>>>=`).
- [x] Add regression coverage for mutation families with language-specific AST nodes (compound assignments now hit augmented-assignment nodes; Go/JS NR coverage kept intact).
- [x] Update tests so they assert every relevant slug and operator token for each language’s compound assignments.
- [x] Run `just test` to confirm all suites pass with the reorganized structure.

Progress update (2026-04-15):
- Merged latest `main` (new NR coverage, augmented-assignment support) and reconciled it with the per-slug test layout.
- Replaced hardcoded compound-assignment slug lists in Go/JavaScript/Rust/Solidity integration tests with language-engine enumeration.
- Updated per-slug tests to assert language-specific operator tokens (e.g., `%=` for Rust/Solidity, `**=` and `>>>=` for JavaScript, `&^=` for Go) and confirmed helpers enforce slug exclusivity.
- Reworked Go helpers/tests to expect AAOS/BAOS/SAOS mutants now that the engine emits them.
- `just test` succeeds locally with the updated suite.

Progress update (2026-04-14):
- Added per-language compound-assignment mutation sub-suites under:
  - `tests/rust/mutations/{aaos,baos,saos}.rs`
  - `tests/solidity/mutations/{aaos,baos,saos}.rs`
  - `tests/javascript/mutations/{aaos,baos,saos}.rs`
  - `tests/go/mutations/{aaos,baos,saos}.rs`
- Added `mutations/mod.rs` in each language and wired module declarations (`tests/*/mod.rs`, plus `tests/go/mod.rs` newly created).
- Kept existing mutation-agnostic integration tests in place and added helpers there.

Verification status (2026-04-14):
- Could not run `just test` / `just pre-commit` in that environment because local Cargo was too old for Rust 2024 edition (`Cargo.toml` parse failure). Needed newer toolchain to verify.
