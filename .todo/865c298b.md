---
id: "865c298b"
title: "backfill-compound-assignment-slug-tests"
---

- [ ] Keep mutation-agnostic coverage in `tests/<lang>/integration_tests.rs`, add mutation-specific suites under `tests/<lang>/mutations/<slug>.rs`, and create the subdirectories as needed.
- [ ] Inline mutation-specific source snippets and expectations in those per-slug tests; keep shared fixtures in `tests/<lang>/examples` for general integration coverage.
- [ ] Introduce a helper that mutates a source snippet, filters by a requested slug, and asserts both the expected mutants appear and no unexpected slugs slip through.
- [ ] For every supported slug in each language, add a dedicated test using that helper to validate the mutants produced by a targeted snippet.
- [ ] Extend the mutation-agnostic integration tests to enumerate supported slugs (e.g., via `mewt print mutations`) and fail if any slug lacks a matching per-slug test file.
- [ ] Run `just test` to confirm the reorganized suites and new assertions all pass.

Progress update (2026-04-14):
- Added per-language compound-assignment mutation sub-suites under:
  - `tests/rust/mutations/{aaos,baos,saos}.rs`
  - `tests/solidity/mutations/{aaos,baos,saos}.rs`
  - `tests/javascript/mutations/{aaos,baos,saos}.rs`
  - `tests/go/mutations/{aaos,baos,saos}.rs`
- Added `mutations/mod.rs` in each language and wired module declarations (`tests/*/mod.rs`, plus `tests/go/mod.rs` newly created).
- Kept existing mutation-agnostic integration tests in place and added helpers there:
  - Rust/Solidity/JavaScript: helper to mutate snippet, filter by requested slug, and assert expected outputs.
  - Go: helper asserting compound-assignment slugs are not generated (engine currently no-ops these).
- Added integration assertions to ensure per-slug compound-assignment test files are present for `AAOS`, `BAOS`, `SAOS`.

Verification status:
- Could not run `just test` / `just pre-commit` in this environment because local Cargo is too old for Rust 2024 edition (`Cargo.toml` parse failure). Needs newer toolchain to verify.
