---
id: "865c298b"
title: "backfill-compound-assignment-slug-tests"
---

- [ ] Audit existing language integration tests (Go, JavaScript/TypeScript/TSX, Rust, Solidity, etc.) to confirm each compound-assignment slug (AAOS, BAOS, SAOS) produces at least one mutant.
- [ ] Add regression coverage for any other mutation families where the AST node kind differs from plain `binary_expression` (e.g., language-specific augmented assignment nodes).
- [ ] Update tests so they assert the presence of every relevant slug and language-specific operator token.
- [ ] Run `just test` to ensure the expanded assertions pass.
